Frequency array
use frequency array to implicitly obtain the sorted property( turn in to tuple and store in dict ) : great for anagram related problems! 
#anagram 
RL : 

Cycle Detection
Instead of actually swapping the elements ,we can detect the minimum number of swaps to achieve a sorted array through cycle detection( through a dictionary tracking the index of sorted element in the original array!)
#dict
RL : 

