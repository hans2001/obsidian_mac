2025-08-25 10:03

Link: https://leetcode.com/problems/top-k-frequent-elements/description/?envType=company&envId=tiktok&favoriteSlug=tiktok-thirty-days

Problem: 
Given an integer array `nums` and an integer `k`, return _the_ `k` _most frequent elements_. You may return the answer in **any order**.

**Example 1:**
**Input:** nums = [1,1,1,2,2,3], k = 2

**Output:** [1,2]

**Example 2:**
**Input:** nums = [1], k = 1

**Output:** [1]

**Example 3:**
**Input:** nums = [1,2,1,2,1,2,3,1,3,2], k = 2

**Output:** [1,2]

**Constraints:**
- `1 <= nums.length <= 105`
- `-104 <= nums[i] <= 104`
- `k` is in the range `[1, the number of unique elements in the array]`.
- It is **guaranteed** that the answer is **unique**.

Intuition:

Solution:
min heap with counter
```python
from collections import Counter
import heapq

def topKFrequent(nums, k):
    freq = Counter(nums)              # O(n)
    heap = []                         # min-heap of (freq, val)

    for val, f in freq.items():       # O(m log k), m = distinct values
        heapq.heappush(heap, (f, val))
        if len(heap) > k:
            heapq.heappop(heap)

    return [val for f, val in heap]
```

max heap 
```python
from collections import Counter
import heapq

def topKFrequent_maxheap(nums, k):
    freq = Counter(nums)
    heap = [(-f, v) for v, f in freq.items()]
    heapq.heapify(heap)               # O(m)
    return [heapq.heappop(heap)[1] for _ in range(k)]
```

Tags: #heap 

RL: 

Considerations:
